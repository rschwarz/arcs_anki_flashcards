[project]
name = "arcs-anki-flashcards"
version = "0.1.0"
description = "Generate Anki flashcards for the board game Arcs."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "genanki>=0.13.1",
    "pillow>=12.1.0",
]
